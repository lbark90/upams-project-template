name: Auto-open PR on push

on:
  push:
    branches-ignore:
      - main
      - 'copilot/**'   # Copilot fix branches handle their own PRs

jobs:
  open-pr:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Open PR if none exists
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const branch = context.ref.replace('refs/heads/', '');
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            // Check if a PR already exists for this branch
            const { data: existing } = await github.rest.pulls.list({
              owner, repo,
              head: `${owner}:${branch}`,
              state: 'open',
            });

            if (existing.length > 0) {
              console.log(`PR already exists for ${branch}: #${existing[0].number}`);
              return;
            }

            // Get the latest commit message for the PR title
            const { data: commit } = await github.rest.repos.getCommit({
              owner, repo, ref: context.sha,
            });
            const title = commit.commit.message.split('
')[0].slice(0, 72);

            const { data: pr } = await github.rest.pulls.create({
              owner, repo,
              title,
              head: branch,
              base: 'main',
              body: `Auto-opened by UPAMS on push to \.

Copilot will review this PR automatically.`,
              draft: false,
            });

            console.log(`Opened PR #${pr.number}: ${pr.html_url}`);
